# Data structure description

The resulting JSON contains information about the available public transport options for ten different destinations. Each destination has a list of possible routes. Each route is described by the total travel time and a list of segments. The list of segments contains the travel time, line number, start stop, end stop, and departure time.